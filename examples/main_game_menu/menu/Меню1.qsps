QSP-Game Меню1

# Меню1
*nl
*nl
*nl
*p'<center><font size=10>'
wait 200
*p'Н'
wait 200
*p'А'
wait 200
*p'З'
wait 200
*p'В'
wait 200
*p'А'
wait 200
*p'Н'
wait 200
*p'И'
wait 200
*p'Е'
wait 200
*p' И'
wait 200
*p'Г'
wait 200
*p'Р'
wait 200
*p'Ы'
'</font>'
*nl
*nl
*nl
wait 1000
'<font size=8>'
*pl'<a href="exec:gt''Начало''">НАЧАТЬ ИГРУ</a>'
wait 1000
*pl'<a href="exec:gt''Начало''">Авторы</a>'
wait 1000
*p'<a href="exec:usehtml=0 & gt''Меню1_код''">Показать код</a>'
'</font></center>'
-- Меню1 ---------------------------------

